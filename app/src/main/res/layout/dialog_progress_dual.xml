<?xml version="1.0" encoding="utf-8"?>
<!-- Dual progress dialog: overall bar (top) + current-file bar (bottom) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="20dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/progress_msg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="@color/text_primary"
        android:singleLine="true"
        android:ellipsize="middle"
        android:minHeight="20dp" />

    <!-- Overall (全量) progress -->
    <ProgressBar
        android:id="@+id/progress_overall"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:progressTint="@color/accent" />

    <TextView
        android:id="@+id/progress_overall_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/text_secondary"
        android:layout_marginTop="2dp" />

    <!-- Current-file progress (bottom bar, shown for large files) -->
    <ProgressBar
        android:id="@+id/progress_file"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:visibility="gone"
        android:progressTint="@color/accent" />

    <TextView
        android:id="@+id/progress_file_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/text_secondary"
        android:layout_marginTop="2dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/progress_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginTop="10dp"
        android:visibility="gone"
        android:textColor="@color/accent"
        android:background="?attr/selectableItemBackground"
        style="?android:attr/borderlessButtonStyle" />

</LinearLayout>
